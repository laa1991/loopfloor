# Contributing

## 报 issue

请写清两件事：

1. **是哪一层的问题** —— 上下文 / 状态 / 目标 / 验收，或某个具体模块。说不清层级的话，先读 README 的 §1 与 §3。
2. **最小复现** —— 一份能跑的最小输入（日志片段、命令、期望与实际）。**没有最小复现的报障，会被当作「待补」而不是「待修」。**

## PR 范围

只收**本仓边界内**的通用件：

- 口径与判据（能复算的读数、能触发的检查）；
- 协议与形状（本仓放的是「该长成什么样」，不是某个具体实现）；
- 失败样本（能证明某条判断错的东西）。

**本仓不搬实现代码**——见 README §0 的边界声明与 §6。

## 边界重申（请勿提交）

凡涉及**心智层 / 记忆实现 / 内部判据集 / 人格核心与提示词注入**的提交，一律不收。这不是保密要求，是**范围**要求：这些东西的价值来自它们所在的那套上下文，搬出来只剩形状而没有承重。

同时**不要**在本仓任何地方写入部署路径、凭据、会话标识或个人身份线索。提交前请自查一遍；CI 之外，本仓的验收清单里也有一条同名检查。

## 两份 README：谁是源

`README.zh.md` 是**源**，`README.md`（英文门面）是它的同步件。

- **改动先改 zh，再同步 en**；固定译法按 `docs/GLOSSARY.zh-en.md`。
- 两份**不是镜像**（en 是门面、zh 是全本），所以机械检查查的是**配对**而不是相等：

  ```sh
  node tools/check-readme-pairing.mjs --repo .
  ```

  任何一边出现**没被声明过的标题**即报红——包括「zh 新增一节、en 没跟上」这种最常见的漂移。新增或改名一节时，同时更新该脚本里的对照表（en-only / zh-only 的节要显式声明并写明理由）。

## 怎么验这个仓（一条可重复的验收，不靠自评）

本仓最常犯的坏法不是「写错了」，而是**用自己的量具量自己**——所有自检都绿，而绿的原因是**那些检查本来就没打算抓这一类坏法**。

⇒ **凡改动落在「主张 / 判据 / 读数」这三类上，发布前跑一次外部读者。** 装置一次几分钟的会话，做法如下。

**做法**：请一个**没见过这个仓**的读者（人或一个零历史的会话），只给它仓本身，让它**只回三行**：

1. **读到了什么** —— 这个仓在讲什么、主张是什么（≤3 句，用自己的话）
2. **拿它做得了什么** —— 具体能做什么、还要自己补什么（≤3 句）
3. **哪里不懂或不信** —— **至少两条，具体到词 / 句 / 数字**：看不懂的词、推不出来的逻辑、不可信的数、**找不到**的东西

**硬要求**（缺一条这个实验就废）：不要夸奖；不评价作者与风格；找不到就说找不到，**不许补一个听起来合理的推断**；第 3 行不许写「整体还算清晰」这类空话。

**为什么非要第 3 行**：实测（2026-09-25）第 1、2 行**基本是在复述材料自己写的东西**——它证明「可读」，不产生新信息；**全部真发现在第 3 行**。那一轮一次找出四处（分母口径对不上、同一状态四处说法不同、判据不承重、术语表死条目），而**当时我们自己的全部检查都是绿的**。

**它会找到什么**：内部不一致、判据不承重、说了但读者核不了的话。
**它找不到什么**：要跑起来才暴露的东西（性能、并发、长跑漂移）。
