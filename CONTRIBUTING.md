# Contributing

## 报 issue

请写清两件事：

1. **是哪一层的问题** —— 上下文 / 状态 / 目标 / 验收，或某个具体模块。说不清层级的话，先读 README 的 §1 与 §3。
2. **最小复现** —— 一份能跑的最小输入（日志片段、命令、期望与实际）。**没有最小复现的报障，会被当作「待补」而不是「待修」。**

## PR 范围

只收**本仓边界内**的通用件：

- 口径与判据（能复算的读数、能开火的检查）；
- 协议与形状（本仓放的是「该长成什么样」，不是某个具体实现）；
- 失败样本（能证明某条判断错的东西）。

**本仓不搬实现代码**——见 README §0 的边界声明与 §6。

## 边界重申（请勿提交）

凡涉及**心智层 / 记忆实现 / 内部判据集 / 人格与提示词注入**的提交，一律不收。这不是保密要求，是**范围**要求：这些东西的价值来自它们所在的那套上下文，搬出来只剩形状而没有承重。

同时**不要**在本仓任何地方写入部署路径、凭据、会话标识或个人身份线索。提交前请自查一遍；CI 之外，本仓的验收清单里也有一条同名检查。
